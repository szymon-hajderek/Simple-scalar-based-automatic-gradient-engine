{
	"version": "2.0.0",
	"tasks": [
    {
      "label": "Configure and Build Current Experiment",
      "type": "shell",
      "command": "cmake -S . -B build && cmake --build build --target ${fileBasenameNoExtension}",
      "group": "build",
      "problemMatcher": ["$gcc"],
      "detail": "Runs CMake configure and then builds the target matching current file"
    }
	]
}